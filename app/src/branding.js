// ---------------------------------------------------------------------------
// BRANDING — company name + logo in ONE place.
// To use the real logo: replace app/assets/netuara-logo.png with your file
// (keep the same name), and it updates everywhere (login, sidebar, header).
// ---------------------------------------------------------------------------

export const COMPANY_NAME = 'Neutara Technologies';

// The image shown as the company logo across the app.
export const companyLogo = require('../assets/netuara-logo.png');

// Central transport-desk helpline shown across the app (Contact Us, and to
// drivers instead of an employee's personal mobile — we never expose a rider's
// private number to the driver). Replace with the real 24×7 number.
export const SUPPORT_HELPLINE = '+91 9848094029';
